services:
  kiro-rs:
    image: ${KIRO_RS_IMAGE:-kiro-rs:opus5}
    container_name: kiro-rs-opus5
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "8991:8991"
    volumes:
      - ./data/:/app/config/
    restart: unless-stopped
